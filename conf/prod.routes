# Add all the application routes to the v1.routes file

->         /                          health.Routes

# NOTE: versioned API routes (e.g. v1.routes) are handled automatically
# via VersionRoutingRequestHandler and so there is no need to include them here

GET        /admin/metrics             com.kenshoo.play.metrics.MetricsController.metrics
